# Deployment examples

Docker Compose is the complete local topology. The Kubernetes example shows a production-style API deployment/readiness probe without embedding credentials. Terraform intentionally provisions only a namespace and does not invent cloud resources disconnected from DecomProof.
