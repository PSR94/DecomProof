# ADR 0004: Fail conservatively

**Status:** accepted

Missing required evidence means `insufficient-evidence`, not clear. Active hard blockers dominate score. Empty verification evidence cannot produce `verified`.
