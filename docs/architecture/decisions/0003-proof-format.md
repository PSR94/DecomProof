# ADR 0003: Versioned proof format

**Status:** accepted

`retirement.proof.json` uses an explicit schema identifier and JSON Schema. Stable evidence IDs, target fingerprints and deterministic ordering allow review and diffing without relying on database identity.
