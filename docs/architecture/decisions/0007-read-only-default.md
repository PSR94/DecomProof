# ADR 0007: Read-only by default

**Status:** accepted

v0.1 analyzes and recommends cleanup but does not delete production infrastructure, mutate databases or unregister consumers. Destructive automation would require a separate capability and explicit approval model.
