# ADR 0001: Rust core

**Status:** accepted

The verdict path is implemented in Rust for deterministic execution, strong types, portable CLI distribution and a small trusted core. Network-facing UI/API code must not duplicate verdict semantics.
