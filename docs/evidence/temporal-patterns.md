# Temporal patterns

The v0.1 temporal engine uses conservative median-gap buckets to describe likely daily, weekly, monthly, approximately quarterly, or rare/irregular behavior. With only two observations, confidence is low. Recommendations extend beyond the observed cadence rather than assuming a short clean period rules out rare use.

This inference informs observation planning. It does not independently make a target ready.
