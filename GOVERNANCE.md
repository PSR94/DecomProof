# Governance

DecomProof currently uses a maintainer-led model. Safety-sensitive changes to verdict semantics, proof compatibility or destructive behavior require an ADR and review. The project favors transparent, deterministic logic over opaque heuristics when a decision can affect deletion safety.
