import { latestProof } from "@/lib/api";import { Header } from "@/components/header";import { EmptyState } from "@/components/empty-state";export default async function Page(){const s=await latestProof();if(!s.proof)return <><Header title="Retirement plan" subtitle="Deterministic remediation derived from blockers."/><EmptyState/></>;return <><Header title="Retirement plan" subtitle="Deterministic remediation derived from blockers." proof={s.proof}/><div className="grid"><div className="card span12"><ol>{s.proof.blockers.map(b=><li key={b.id}><strong>{b.kind}</strong>: {b.message}</li>)}</ol>{!s.proof.blockers.length?<p>No blocker-derived remediation remains. Complete review and removal procedure.</p>:null}</div></div></>}
