import { SectionPage } from "@/components/section-page"; export default function Page(){return <SectionPage title="Scheduled consumers" subtitle="Cron, Kubernetes CronJob and workflow schedule references." prefixes={["schedule.","ci."]}/>}
