import { listTargets } from "@/lib/api"; import { Header } from "@/components/header"; import { ApiError, EmptyState } from "@/components/empty-state";
export default async function Projects(){try{const targets=await listTargets();const projects=[...new Set(targets.map(t=>t.project_id))];return <><Header title="Projects" subtitle="Repositories and decommission targets grouped by project."/><div className="grid"><div className="card span12">{projects.length?<table className="table"><thead><tr><th>Project ID</th><th>Targets</th></tr></thead><tbody>{projects.map(id=><tr key={id}><td>{id}</td><td>{targets.filter(t=>t.project_id===id).length}</td></tr>)}</tbody></table>:<EmptyState/>}</div></div></>}catch(e){return <><Header title="Projects" subtitle="Repositories and decommission targets grouped by project."/><ApiError message={String(e)}/></>}}
