import { latestProof } from "@/lib/api";import { Header } from "@/components/header";import { EmptyState } from "@/components/empty-state";export default async function Page(){const s=await latestProof();if(!s.proof)return <><Header title="Proof artifact viewer" subtitle="Canonical structured evidence package."/><EmptyState/></>;return <><Header title="Proof artifact viewer" subtitle={`Schema ${s.proof.schema} · policy ${s.proof.policy_version}`} proof={s.proof}/><div className="grid"><div className="card span12 proof">{JSON.stringify(s.proof,null,2)}</div></div></>}
