openapi: 3.1.0
info: {title: AtlasCommerce API, version: 1.0.0}
paths:
  /v1/legacy-export:
    post:
      operationId: legacy-export
      deprecated: true
      responses:
        '200': {description: legacy export}
